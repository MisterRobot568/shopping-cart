import { useParams } from "react-router-dom"; // so that we can dynamically swap pages

const Mens = () => {
  return (
    <div>
      <p>MENS SELECTION </p>
    </div>
  );
};

export default Mens;
